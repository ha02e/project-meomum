<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.mm.point">

<!-- 	<select id="poinTotal" parameterType="Integer" resultType="Integer">
		SELECT distinct
	  		(SELECT sum(point_num) FROM point WHERE user_idx = #{user_idx} AND point_use = 0) -
	  		(SELECT sum(point_num) FROM point WHERE user_idx = #{user_idx} AND point_use = 1) 
		FROM point
	</select>  -->
	
	
<!-- 	<select id="pointTotal" parameterType="Integer" resultType="com.mm.point.model.ResultDTO">
		SELECT distinct
	  		(SELECT sum(point_num) FROM point WHERE user_idx = #{user_idx} AND point_use = 0) -
	  		(SELECT sum(point_num) FROM point WHERE user_idx = #{user_idx} AND point_use = 1) as result
		FROM point
	</select>  -->

</mapper>